[tool.poetry]
name = "antarctic"
version = "0.0.0"
description = "..."
authors = ["Thomas Schmelzer"]
readme = "readme.md"
repository = "https://github.com/tschm/antarctic"
packages = [{include = "antarctic"}]

[tool.poetry.dependencies]
python = ">=3.9.0"
mongoengine= "*"
pandas = "*"
pyarrow = "*"
fastparquet= "*"

[tool.poetry.dev-dependencies]
pytest = "7.1.3"
pytest-cov = "*"
mongomock = "*"

[build-system]
requires = ["poetry>=1.0.2"]
build-backend = "poetry.masonry.api"

