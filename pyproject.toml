[tool.poetry]
name = "antarctic"
version = "0.0.0"
description = "..."
authors = ["Thomas Schmelzer"]
readme = "readme.md"
repository = "https://github.com/tschm/antarctic"
packages = [{include = "antarctic"}]
homepage = "https://tschm.github.io/antarctic"

[tool.poetry.dependencies]
python = ">=3.9.0"
mongoengine= "*"
pandas = "*"
pyarrow = "*"
fastparquet= "*"

[tool.poetry.group.test.dependencies]
pytest = "8.3.3"
pytest-cov = "*"
mongomock = "*"
pre-commit = "*"

[build-system]
requires = ["poetry>=1.6.0"]
build-backend = "poetry.masonry.api"
